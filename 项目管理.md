# AvalonBase 项目管理

> 一个 Minecraft 1.20.1 的通用前置库（支持库），为其他模组提供 GUI 主题系统、配置框架、网络框架与平台抽象。

---

## 1. 项目概览

| 项目 | 值 |
|---|---|
| Mod ID | `avalonbase` |
| 当前版本 | `1.1.0` |
| 游戏版本 | Minecraft `1.20.1` |
| 支持平台 | Forge `47.2.30` / Fabric Loader `0.16.9` |
| Java | 17 |
| License | MIT |
| 作者 | Huziyang520 |
| 定位 | **被依赖方**（前置库），不提供独立玩法 |

### 项目职责
为 `OnlyTP`、`AuthCmd` 等模组提供可复用的通用能力，避免各模组重复实现：
- **GUI 主题系统** `gui/theme`：纯代码绘制、多配色主题（末影紫/海洋蓝/翡翠绿/熔岩红/米白）
- **配置界面基类** `gui/screen`：现成的配置页模板
- **光标工具** `gui/GuiCursor`：统一管理手形/箭头光标
- **配置读写** `config/AvalonToml`：TOML 存取 + 热加载
- **网络抽象** `network/AvalonNetwork`：跨平台消息封装
- **平台服务** `platform/Services`：判断 Forge/Fabric 与模组加载情况

---

## 2. 模块结构（MultiLoader）

```
AvalonBase/1.20.1/
├─ common/     # 平台无关共享代码（GUI/配置/网络抽象/平台接口）
├─ fabric/     # Fabric 平台实现（网络、平台 helper、模组入口）
├─ forge/      # Forge 平台实现（网络、平台 helper、模组入口）
├─ buildSrc/   # 自定义 Gradle 插件（multiloader-common / loader）
├─ build.gradle / settings.gradle / gradle.properties
└─ gradlew[.bat] / gradle/
```

> 构建说明：本库是前置库，需先构建生成 jar（`common` 出 dev jar、`fabric`/`forge` 出可运行 mod jar），下游模组再引用。

---

## 3. 源码文件关系

### common 模块（`com.avalon.base`，20 个文件）
| 包 | 文件 | 职责 |
|---|---|---|
| 根 | `Constants` | mod_id / 名称 / Logger |
| `config` | `AvalonToml` | TOML 配置读写与热加载 |
| `config` | `ThemeConfig` | 主题选择的持久化 |
| `gui` | `GuiCursor` | 光标工具（手形/箭头） |
| `gui` | `Themes` | 主题全局门面（设置/读取预设） |
| `gui/screen` | `AvalonConfigScreen` | 配置界面基类（保存/取消/滚动） |
| `gui/theme` | `GuiTheme` | 主题接口 |
| `gui/theme` | `ModernTheme` / `VanillaTheme` | 现代 / 原版风格实现 |
| `gui/theme` | `ThemedButton` / `ThemedToggle` / `ThemedRadio` | 主题化控件 |
| `gui/theme` | `Palette` | 24 色槽配色盘 |
| `gui/theme` | `ThemePreset` / `ThemePresetPicker` | 预设定义 + 换色控件 |
| `gui/theme` | `Anim` | 悬停/动画过渡 |
| `network` | `AvalonNetwork` | 网络门面（向调用方暴露注册/发送） |
| `network` | `INetworkHandler` | 平台无关网络接口（含 `MessageContext`） |
| `platform` | `Services` | 平台门面 |
| `platform/services` | `IPlatformHelper` | 平台能力接口 |

### fabric 模块（4 个文件）
| 文件 | 职责 |
|---|---|
| `AvalonBaseMod` | Fabric 模组入口 |
| `client/AvalonBaseClient` | Fabric 客户端初始化 |
| `network/FabricNetworkHandler` | `INetworkHandler` 的 Fabric 实现 |
| `platform/FabricPlatformHelper` | `IPlatformHelper` 的 Fabric 实现 |

### forge 模块（3 个文件）
| 文件 | 职责 |
|---|---|
| `AvalonBaseMod` | Forge 模组入口 |
| `network/ForgeNetworkHandler` | `INetworkHandler` 的 Forge 实现（SimpleChannel） |
| `platform/ForgePlatformHelper` | `IPlatformHelper` 的 Forge 实现 |

### 依赖关系
- `AvalonBase` **不依赖** `OnlyTP` / `AuthCmd`。
- `OnlyTP`、`AuthCmd` **依赖** `AvalonBase`（编译期引用其 fabric dev jar / forge jar，运行期作为前置 mod）。
- 平台实现依赖关系：`fabric`/`forge` → `common`（`commonJava` 配置引用 common 源码）。

---

## 4. 版本与兼容性

| 项 | 版本 | 备注 |
|---|---|---|
| 库版本 | `1.1.0` | 下游 `OnlyTP`/`AuthCmd` 需同步引用 |
| Minecraft 范围 | `[1.20.1, 1.22)` | |
| Parchment | `2023.09.03` | |
| Fabric API | `0.92.1+1.20.1` | |
| 网络协议版本 | `"1"`（Forge） | 前后端协议版本须一致 |

> ⚠️ **版本同步提醒**：下游模组构建文件中对 AvalonBase 的 jar 文件名写死了版本号。本库版本升级时，务必同步更新 `OnlyTP` 与 `AuthCmd` 的 `build.gradle` 依赖（详见各项目文档）。

---

## 5. 进度追踪（Tasks）

| 状态 | 任务 | 说明 |
|---|---|---|
| ✅ 完成 | 主题系统（5 套配色 + 自定义 Palette） | 核心能力 |
| ✅ 完成 | 主题化控件（按钮/开关/单选） | 纯代码渲染 |
| ✅ 完成 | 配置界面基类 `AvalonConfigScreen` | 模板化配置页 |
| ✅ 完成 | TOML 配置读写 + 热加载 | |
| ✅ 完成 | 跨平台网络抽象 | Forge/Fabric 双实现 |
| ✅ 完成 | 平台服务判断 | |
| ✅ 完成 | 光标工具 `GuiCursor` | 手形/箭头切换 |
| 🔲 待定 | 版本 `1.1.0` 发布 / 下游同步 | 需联动 OnlyTP/AuthCmd |

---

## 6. 决策记录（Decision Log）

| 日期 | 决策 | 理由 / 结果 |
|---|---|---|
| - | 采用 MultiLoader 三模块结构 | 一套 common 源码同时支持 Forge/Fabric |
| - | 网络层用平台无关抽象 `INetworkHandler` | 让下游模组统一 API，不感知平台差异 |
| - | 光标工具「悬停置手形、离开不主动写箭头」 | 避免多按钮每帧互相覆盖光标、干扰原版机制 |
| 2026-08-16 | 版本从 `1.0.0` 提升到 `1.1.0` | 需同步更新 OnlyTP/AuthCmd 的依赖引用 |

---

## 7. 构建说明

```bash
# 在 AvalonBase/1.20.1 目录下
./gradlew build          # 全量构建（common dev jar + fabric/forge 可运行 jar）
./gradlew :fabric:build  # 仅构建 fabric
./gradlew :forge:build   # 仅构建 forge
```

> 构建产物：`fabric/build/libs/avalonbase-fabric-*.jar`、`forge/build/libs/avalonbase-forge-*.jar`、`common/build/devlibs/avalonbase-*-dev.jar`。
